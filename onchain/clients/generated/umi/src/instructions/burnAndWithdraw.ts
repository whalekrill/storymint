/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type BurnAndWithdrawInstructionAccounts = {
  owner: Signer;
  asset: PublicKey | Pda;
  collection: PublicKey | Pda;
  masterState?: PublicKey | Pda;
  vault?: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
  logWrapper?: PublicKey | Pda;
  mplCore?: PublicKey | Pda;
};

// Data.
export type BurnAndWithdrawInstructionData = { discriminator: Uint8Array };

export type BurnAndWithdrawInstructionDataArgs = {};

export function getBurnAndWithdrawInstructionDataSerializer(): Serializer<
  BurnAndWithdrawInstructionDataArgs,
  BurnAndWithdrawInstructionData
> {
  return mapSerializer<
    BurnAndWithdrawInstructionDataArgs,
    any,
    BurnAndWithdrawInstructionData
  >(
    struct<BurnAndWithdrawInstructionData>(
      [['discriminator', bytes({ size: 8 })]],
      { description: 'BurnAndWithdrawInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([216, 117, 35, 35, 239, 192, 140, 79]),
    })
  ) as Serializer<
    BurnAndWithdrawInstructionDataArgs,
    BurnAndWithdrawInstructionData
  >;
}

// Instruction.
export function burnAndWithdraw(
  context: Pick<Context, 'eddsa' | 'programs'>,
  input: BurnAndWithdrawInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'lockedSolPnft',
    '3kLyy6249ZFsZyG74b6eSwuvDUVndkFM54cvK8gnietr'
  );

  // Accounts.
  const resolvedAccounts = {
    owner: {
      index: 0,
      isWritable: true as boolean,
      value: input.owner ?? null,
    },
    asset: {
      index: 1,
      isWritable: true as boolean,
      value: input.asset ?? null,
    },
    collection: {
      index: 2,
      isWritable: true as boolean,
      value: input.collection ?? null,
    },
    masterState: {
      index: 3,
      isWritable: true as boolean,
      value: input.masterState ?? null,
    },
    vault: {
      index: 4,
      isWritable: true as boolean,
      value: input.vault ?? null,
    },
    systemProgram: {
      index: 5,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
    logWrapper: {
      index: 6,
      isWritable: false as boolean,
      value: input.logWrapper ?? null,
    },
    mplCore: {
      index: 7,
      isWritable: false as boolean,
      value: input.mplCore ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.masterState.value) {
    resolvedAccounts.masterState.value = context.eddsa.findPda(programId, [
      bytes().serialize(new Uint8Array([109, 97, 115, 116, 101, 114])),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.collection.value)
      ),
    ]);
  }
  if (!resolvedAccounts.vault.value) {
    resolvedAccounts.vault.value = context.eddsa.findPda(programId, [
      bytes().serialize(new Uint8Array([118, 97, 117, 108, 116])),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.asset.value)
      ),
    ]);
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'systemProgram',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  if (!resolvedAccounts.mplCore.value) {
    resolvedAccounts.mplCore.value = context.programs.getPublicKey(
      'mplCore',
      'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'
    );
    resolvedAccounts.mplCore.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getBurnAndWithdrawInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
