/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';
import {
  CollectionArgs,
  CollectionArgsArgs,
  getCollectionArgsSerializer,
} from '../types';

// Accounts.
export type InitializeCollectionInstructionAccounts = {
  payer: Signer;
  masterState?: PublicKey | Pda;
  mintAuthority?: PublicKey | Pda;
  collection: Signer;
  /** CHECK Server authority */
  updateAuthority: Signer;
  systemProgram?: PublicKey | Pda;
  mplCore?: PublicKey | Pda;
};

// Data.
export type InitializeCollectionInstructionData = {
  discriminator: Uint8Array;
  args: CollectionArgs;
};

export type InitializeCollectionInstructionDataArgs = {
  args: CollectionArgsArgs;
};

export function getInitializeCollectionInstructionDataSerializer(): Serializer<
  InitializeCollectionInstructionDataArgs,
  InitializeCollectionInstructionData
> {
  return mapSerializer<
    InitializeCollectionInstructionDataArgs,
    any,
    InitializeCollectionInstructionData
  >(
    struct<InitializeCollectionInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['args', getCollectionArgsSerializer()],
      ],
      { description: 'InitializeCollectionInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([112, 62, 53, 139, 173, 152, 98, 93]),
    })
  ) as Serializer<
    InitializeCollectionInstructionDataArgs,
    InitializeCollectionInstructionData
  >;
}

// Args.
export type InitializeCollectionInstructionArgs =
  InitializeCollectionInstructionDataArgs;

// Instruction.
export function initializeCollection(
  context: Pick<Context, 'eddsa' | 'programs'>,
  input: InitializeCollectionInstructionAccounts &
    InitializeCollectionInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'storymint',
    '5qGtv9tb8yWuLG6jm2EPTBa3SsLP2hkhToB7tZT6mp5B'
  );

  // Accounts.
  const resolvedAccounts = {
    payer: {
      index: 0,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    masterState: {
      index: 1,
      isWritable: true as boolean,
      value: input.masterState ?? null,
    },
    mintAuthority: {
      index: 2,
      isWritable: true as boolean,
      value: input.mintAuthority ?? null,
    },
    collection: {
      index: 3,
      isWritable: true as boolean,
      value: input.collection ?? null,
    },
    updateAuthority: {
      index: 4,
      isWritable: true as boolean,
      value: input.updateAuthority ?? null,
    },
    systemProgram: {
      index: 5,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
    mplCore: {
      index: 6,
      isWritable: false as boolean,
      value: input.mplCore ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitializeCollectionInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.masterState.value) {
    resolvedAccounts.masterState.value = context.eddsa.findPda(programId, [
      bytes().serialize(new Uint8Array([109, 97, 115, 116, 101, 114])),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.collection.value)
      ),
    ]);
  }
  if (!resolvedAccounts.mintAuthority.value) {
    resolvedAccounts.mintAuthority.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([
          109, 105, 110, 116, 95, 97, 117, 116, 104, 111, 114, 105, 116, 121,
        ])
      ),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.collection.value)
      ),
    ]);
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'systemProgram',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  if (!resolvedAccounts.mplCore.value) {
    resolvedAccounts.mplCore.value = context.programs.getPublicKey(
      'mplCore',
      'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'
    );
    resolvedAccounts.mplCore.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getInitializeCollectionInstructionDataSerializer().serialize(
    resolvedArgs as InitializeCollectionInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
